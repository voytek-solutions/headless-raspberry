# {{ ansible_managed }}
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

{% for network in raspbian_headless.networks %}
network={
{% for key, value in network.items() %}
    {{ key }}={{ value }}
{% endfor %}
}
{% endfor %}
